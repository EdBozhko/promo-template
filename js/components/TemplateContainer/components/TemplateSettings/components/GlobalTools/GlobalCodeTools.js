class GlobalCodeTools extends Tools {
  #templateContainer
  constructor(parent, index, templateContainer) {
    super(parent, index, templateContainer)
    this.#templateContainer = templateContainer
  }

  render(container) {
    super.render(container)
  }
}
